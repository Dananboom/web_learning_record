# 构成js的砖块

## 比较运算符

### === 和 !== — 判断一个值是否严格等于，或不等于另一个。（类型也要相等）

### < 和 > — 判断一个值是否小于，或大于另一个。

### <= 和 >= — 判断一个值是否小于或等于，或者大于或等于另一个。

## 逻辑运算符

### && — 逻辑与; 使得并列两个或者更多的表达式成为可能，只有当这些表达式每一个都返回true时，整个表达式才会返回true.

### || — 逻辑或; 当两个或者更多表达式当中的任何一个返回 true 则整个表达式将会返回 true.

### !  — 逻辑非; 对一个布尔值取反, 非true返回false,非false返回true.

## 函数

### 浏览器内部函数

### 函数与方法

- 函数是方法的一部分
- 严格说来，内置浏览器函数并不是函数——它们是方法。

### 自定义函数

- function 开头定义一个函数

### 调用函数

### 匿名函数

- 通常用来响应事件的触发
- 匿名函数也称函数表达式，函数声明会变量提升，但是函数表达式不会

### 函数参数

- 也叫做属性

### 函数和作用域

### 函数返回值

## 事件

### 事件对象

- 在事件调用的函数里，有个默认的参数event或者evt或者e，他就是事件处理对象，被自动传给事件处理函数

### 阻止默认事件

- preventDefault

### 事件的冒泡和捕获

- 冒泡

	- 先在实际被点击的元素中找找是不是有被注册事件的元素

		- 在现代浏览器中，默认情况下，所有事件处理程序都在冒泡阶段进行注册。

			- stopPropagation用这个函数可以防止冒泡变大，找到他的父元素

- 捕获

	- 从html，body这些标签开始找被注册事件的元素

### 事件委托

- 假如好多的代码被触发事件以后都是一样的结果，那可以把事件设置到父元素上，实际点击子元素会把事件冒泡到父元素上

*XMind - Trial Version*